{
  "name": "baidu-autosave-frontend",
  "version": "1.1.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "build:prod": "npx vite build",
    "preview": "vite preview",
    "version:patch": "npm version patch --no-git-tag-version && git add package.json && git commit -m \"chore: bump version to $(jq -r '.version' package.json)\" && git tag \"v$(jq -r '.version' package.json)\" && git push && git push --tags",
    "version:minor": "npm version minor --no-git-tag-version && git add package.json && git commit -m \"chore: bump version to $(jq -r '.version' package.json)\" && git tag \"v$(jq -r '.version' package.json)\" && git push && git push --tags",
    "version:major": "npm version major --no-git-tag-version && git add package.json && git commit -m \"chore: bump version to $(jq -r '.version' package.json)\" && git tag \"v$(jq -r '.version' package.json)\" && git push && git push --tags",
    "build:version": "echo $npm_package_version > public/version.txt && vite build"
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.1.0",
    "@vueuse/core": "^10.7.0",
    "element-plus": "^2.4.0",
    "pinia": "^2.1.0",
    "sortablejs": "^1.15.0",
    "vue": "^3.4.0",
    "vue-router": "^4.2.0",
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/sortablejs": "^1.15.0",
    "@vitejs/plugin-vue": "^4.5.0",
    "typescript": "^5.2.0",
    "unplugin-auto-import": "^0.17.0",
    "unplugin-vue-components": "^0.26.0",
    "vite": "^5.0.0",
    "vue-tsc": "^3.0.7"
  }
}
